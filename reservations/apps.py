
from django.apps import AppConfig


class ReservationsConfig(AppConfig):
    """Metadata for the *reservations* application."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "reservations"

